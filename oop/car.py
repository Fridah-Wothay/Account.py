class Car:
      name = "BMW"
      def _init_(self,car_name, car_model, car_color, car_capacity,car_number):
                self.car_name = car_name
                self.car_model = car_model
                self.car_color = car_color
                self.car_capacity = car_capacity
                self.car_number = car_number
