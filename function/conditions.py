def print_numbers(n);
    numbers= range(n)
    for number in  numbers:
        print(number)

def print_even_numbers(n);
    number = range(n)  
    for numbers in numbers:
        if number % 2 == 0:
            print(number)      